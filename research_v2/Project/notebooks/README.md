# Notebook Workspace

This folder is reserved for short local validation notebooks and one-off
inspection scripts during the v2 rebuild.

Primary experiment execution should still happen through the structured
`experiments/` entrypoints rather than notebooks.
