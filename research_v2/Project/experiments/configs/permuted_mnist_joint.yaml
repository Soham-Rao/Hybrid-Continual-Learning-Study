# Permuted MNIST — Joint Training (upper bound)
dataset: permuted_mnist
model: resnet8
method: joint_training
n_epochs: 1
batch_size: 32
fp16: false
joint_replay_epochs: 1
seeds: [42, 123, 456, 789, 1024]
