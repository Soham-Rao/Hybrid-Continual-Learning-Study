# Permuted MNIST — A-GEM (gradient projection baseline)
dataset: permuted_mnist
dataset_image_size: 32
model: slim_resnet18
method: agem
buffer_size: 200
agem_mem_batch: 64
n_epochs: 1
batch_size: 32
num_workers: 2
fp16: false
seeds: [42, 123, 456, 789, 1024]
