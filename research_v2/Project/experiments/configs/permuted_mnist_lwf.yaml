# Permuted MNIST — LwF (distillation-only baseline)
dataset: permuted_mnist
dataset_image_size: 32
model: slim_resnet18
method: lwf
lwf_lambda: 1.0
lwf_temp: 2.0
n_epochs: 1
batch_size: 32
num_workers: 2
fp16: false
seeds: [42, 123, 456, 789, 1024]
