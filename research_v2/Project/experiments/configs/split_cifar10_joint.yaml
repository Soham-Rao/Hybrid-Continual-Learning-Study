# Split CIFAR-10 — Joint Training (upper bound)
dataset: split_cifar10
model: slim_resnet18
method: joint_training
n_epochs: 1
batch_size: 32
lr: 0.03
fp16: false
joint_replay_epochs: 1
seeds: [42, 123, 456, 789, 1024]
