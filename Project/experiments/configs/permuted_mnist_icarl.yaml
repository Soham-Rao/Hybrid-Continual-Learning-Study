# Permuted MNIST — ICARL (Phase 3 hybrid)
dataset: permuted_mnist
model: resnet8
method: icarl
n_epochs: 1
batch_size: 32
lr: 0.03
fp16: false
buffer_size: 500
icarl_distill_w: 1.0
icarl_temp: 2.0
use_nmc: true
seeds: [42, 123, 456, 789, 1024]
