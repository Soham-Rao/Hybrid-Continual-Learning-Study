# Split CIFAR-10 — A-GEM (gradient projection baseline)
dataset: split_cifar10
model: slim_resnet18
method: agem
buffer_size: 200
agem_mem_batch: 64
n_epochs: 1
batch_size: 32
lr: 0.03
fp16: false
seeds: [42, 123, 456, 789, 1024]
