# Split CIFAR-10 — complete set of experiment configs
# This file runs the full method comparison on CIFAR-10 (5 tasks × 2 classes)
# Run individually: python experiments/run_experiment.py --config this_file.yaml --seed 42

# --- Baseline: Fine-Tune ---
dataset: split_cifar10
model: slim_resnet18
method: fine_tune
n_epochs: 1
batch_size: 32
lr: 0.03
fp16: false
seeds: [42, 123, 456, 789, 1024]
